{
  "name": "stockpro",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@10.17.0",
  "scripts": {
    "start": "concurrently -n backend,tunnel -c blue,magenta \"pnpm run start:backend\" \"pnpm run start:tunnel\"",
    "start:backend": "cd backend && pnpm run dev",
    "start:tunnel": "node backend/start-tunnel.js",
    "dev": "concurrently -n backend,frontend -c blue,green \"pnpm run start:backend\" \"pnpm run dev:frontend\"",
    "dev:frontend": "cd frontend && pnpm run dev",
    "build": "cd frontend && pnpm install && pnpm run build"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
